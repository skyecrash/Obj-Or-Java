/* Lilith Freed, laf70 */

/* Fractal Class */

public class Fractal {
  
  /* Fields */
  private Line line;
  private int layers;
  private Line line1, line2, line3, line4;
  
  /* Constructors */
  public Fractal(Line line, int layers) {
    this.line = line;
    this.layers = layers;
    
    int x1 = (line.getFirstPoint()).getX();
    int y1 = (line.getFirstPoint()).getY();
    int x2 = (line.getSecondPoint()).getX();
    int y2 = (line.getSecondPoint()).getY();
    
    Point p2 = new Point((x2 - x1) / 3 + x1, (y2 - y1) / 3 + y1);
    Point p4 = new Point(x2 - (x2 - x1) / 3, y2 - (y2 - y1) / 3);
    
    double d = (
    
    
    Point p3 = new Point(
  }
  
  /* WTF IS THIS SHIT */
  =
    